// Search metadata for ReturnRefundPolicyPage, decoupled from the page module.
// Consumed by the site search registry.
import type { PageSearchMeta } from '../config/siteSearchTypes'

export const PAGE_SEARCH_META: PageSearchMeta = {
  id: 'page-return-refund-policy',
  kind: 'site',
  title: 'Return & Refund Policy',
  body:
    'Returns and refunds: AC Drain Wiz products sold through authorized distributors; returns handled by point of purchase. Distributor policy, satisfaction, authorized reseller channel.',
  tags: ['return', 'refund', 'distributor', 'purchase'],
  href: '/return-refund-policy',
}
